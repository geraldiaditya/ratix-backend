meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "name": "Bob",
    "email": "bob@example.com",
    "password": "secret",
    "confirm_password": "secret"
  }
}
